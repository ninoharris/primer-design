import react from 'react'
import styled from 'styled-components'

export const Sequence = styled.span`
  font-family: monospace;
  white-space: pre-wrap;
  line-height;
  > span {
    display: inline;
  }
`

export const FVSequence = styled.span`
  
`